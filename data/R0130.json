{
  "id": "R0130",
  "name": "VacuumTube",
  "category": "YouTube app",
  "historical_tier": "C",
  "project_url": "https://github.com/shy1132/VacuumTube",
  "channel": "primary",
  "review_status": "Original Claude Code analysis",
  "proposed_primary_tier": "C",
  "description": "electron-updater SHA512, no code-signing config found.",
  "limitations": [
    "AI-generated static source analysis. Confirm the affected version, platform, update channel, and cited control flow before relying on this classification."
  ],
  "sources": [
    {
      "url": "https://github.com/shy1132/VacuumTube",
      "commit": "0365dead79d295294855f3dcc2f71702c0f8c427"
    }
  ],
  "review_basis": "original-claude-code-analysis",
  "snapshot_date": "2026-09-08",
  "classification_certified": false,
  "later_review_audit": {
    "description": "VacuumTube uses electron-updater's checkForUpdatesAndNotify after startup and configures the shy1132/VacuumTube GitHub publisher for Windows and macOS artifacts. The reviewed package configuration has no explicit code-signing or notarization setting; electron-updater's SHA-512 behavior is dependency-provided and not independently established here.",
    "limitations": [
      "Newly retrieved default-branch snapshot, not the original study checkout; release artifacts and builder environment are not audited.",
      "electron-updater internals and generated latest.yml/SHA-512 metadata were not part of review, so SHA-512 enforcement remains dependency-level evidence only.",
      "Newly retrieved default-branch snapshot, not the original study checkout; release artifacts and builder environment are not audited.; electron-updater internals and generated latest.yml/SHA-512 metadata were not part of review, so SHA-512 enforcement remains dependency-level evidence only."
    ],
    "review_status": "Revised description",
    "review_basis": "publication-decision",
    "proposed_primary_tier": "UNRESOLVED",
    "classification_certified": false,
    "status": "superseded-as-primary-after-systematic-source-retrieval-failures"
  },
  "downloads_and_runs": "yes",
  "transport": "https (GitHub)",
  "payload_verification": "hash (SHA512, no code-sign)",
  "trust_root": "server-TLS",
  "original_evidence": [
    {
      "file": "batch-I5.md",
      "line": 16,
      "text": "| VacuumTube | github.com/shy1132/VacuumTube (active forks exist, e.g. FOSSforlife/VacuumTube) | Electron | Y | chk + dl automatic on every launch (`autoUpdater.checkForUpdatesAndNotify()`); inst deferred to user restart (standard electron-updater UX) | Y (downloads new release asset; install/replace happens on app restart via electron-updater) | https via GitHub Releases (`build.publish: {\"provider\":\"github\",\"owner\":\"shy1132\",\"repo\":\"VacuumTube\"}`); TLS enforced (Node https default, no override found) | N (`latest.yml`/`latest-mac.yml`/`latest-linux.yml` are auto-generated by electron-builder alongside the binaries — not separately signed) | hash-from-manifest (electron-updater's standard SHA512 field inside `latest*.yml`, compared post-download) | N/A (hash only; no `certificateFile`/Windows code-signing block configured in package.json `build.win`, so no OS-code-signing check layered on top) | src/index.js:3 (`const { autoUpdater } = require('electron-updater')`), :129 (`autoUpdater.checkForUpdatesAndNotify()`, called right after `app.whenReady()`); package.json `build.publish` (`provider: github`) |"
    }
  ]
}
