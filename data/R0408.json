{
  "id": "R0408",
  "name": "Wick Editor",
  "category": "Animation tool (Electron)",
  "historical_tier": "B",
  "project_url": "https://github.com/Wicklets/wick-editor",
  "channel": "primary",
  "review_status": "Original Claude Code analysis",
  "proposed_primary_tier": "B",
  "description": "electron-updater auto-download+install; macOS notarized (B), Windows shows no code-signing (same-origin sha512 = C leg). Likely abandoned (2021).",
  "limitations": [
    "AI-generated static source analysis. Confirm the affected version, platform, update channel, and cited control flow before relying on this classification."
  ],
  "sources": [
    {
      "url": "https://github.com/Wicklets/wick-editor",
      "commit": "f34f0d9512d7165e74c1910ea1aba9173ab8dec2"
    }
  ],
  "review_basis": "original-claude-code-analysis",
  "snapshot_date": "2026-09-08",
  "classification_certified": false,
  "later_review_audit": {
    "description": "In the reviewed Wick Editor snapshot, the Electron main process calls electron-updater checkForUpdatesAndNotify at ready. Its builder config includes an afterSign notarization hook and hardened macOS settings; the checked Windows block specifies a target icon but does not establish released artifact signing or updater verification.",
    "limitations": [
      "New default-branch snapshot rather than original study checkout; release artifacts, generated manifests, Windows signing, and external electron-updater enforcement remain unverified."
    ],
    "review_status": "Revised description",
    "review_basis": "publication-decision",
    "proposed_primary_tier": "UNRESOLVED",
    "classification_certified": false,
    "status": "superseded-as-primary-after-systematic-source-retrieval-failures"
  },
  "downloads_and_runs": "auto",
  "transport": "https",
  "payload_verification": "sha512 + OS code-signing (mac)",
  "trust_root": "electron-updater",
  "original_evidence": [
    {
      "file": "batch-AA3.md",
      "line": 17,
      "text": "| Wick Editor | https://github.com/Wicklets/wick-editor | f34f0d9 / 2021-01-20 (stale — repo appears inactive) | Y | Y | Y | https, TLS validation not disabled (no rejectUnauthorized/InsecureSkipVerify etc. found) | N (no signed manifest; electron-builder auto-generates unsigned `latest.yml`/`latest-mac.yml` with sha512, not committed to repo) | hash (sha512 in electron-builder-generated yml) + macOS: Gatekeeper/notarization via `afterSign: electron-builder-notarize` (`package.json:62`); Windows: no CSC/certificateFile config found in repo → signing status unknown/likely unsigned | mac: Apple notarization (OS-anchored); win: unpinned/server-supplied (hash comes from same GitHub Releases origin electron-updater fetches from) | `public/electron.js:2` (`require(\"electron-updater\")`), `:29,67` (`autoUpdater.autoUpdater.checkForUpdatesAndNotify()`); `package.json:21` (electron-updater dep), `:8-10` (repository → implicit GitHub Releases publish provider), `:62` (afterSign notarize), `:68-71` (nsis oneClick:false, no signing cert refs) |"
    },
    {
      "file": "batch-AL6.md",
      "line": 14,
      "text": "| Wick Editor | https://github.com/Wicklets/wick-editor.git | f34f0d95 / 2021-01-20 (stale — 5+ yrs old, effectively unmaintained) | Y | Y (`checkForUpdatesAndNotify()` — electron-updater default auto-download) | Y | https (GitHub Releases via electron-updater provider, inferred from `repository` field) | B-style — electron-updater verifies OS code-signing (Authenticode/notarization) of the downloaded package, not an app-level detached signature | OS code-signing (electron-updater + electron-builder-notarize) | OS keystore / Apple notarization & Windows Authenticode chain (not a key pinned in this app's own binary) | public/electron.js:2 (`require(\"electron-updater\")`), :67 (`autoUpdater.checkForUpdatesAndNotify()`), package.json:21,58-62 (electron-updater, electron-builder-notarize), package.json:8-10 (repository → GitHub provider) |"
    }
  ]
}
