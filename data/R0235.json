{
  "id": "R0235",
  "name": "Requestly",
  "category": "API interceptor",
  "historical_tier": "B",
  "project_url": "https://github.com/requestly/http-interceptor-desktop-app",
  "channel": "primary",
  "review_status": "Original Claude Code analysis",
  "proposed_primary_tier": "B",
  "description": "electron-updater defaults intact (auto-check depends on remote web UI).",
  "limitations": [
    "AI-generated static source analysis. Confirm the affected version, platform, update channel, and cited control flow before relying on this classification."
  ],
  "sources": [],
  "review_basis": "original-claude-code-analysis",
  "snapshot_date": "2026-09-08",
  "classification_certified": false,
  "project_link_basis": "Recorded repository in original batch report; not a new upstream verification.",
  "later_review_audit": {
    "description": "The recovered Requestly candidate does not contain the desktop updater implementation or package configuration needed to substantiate electron-updater defaults; updater behavior remains unresolved.",
    "limitations": [
      "The recovered default-branch candidate is a newly retrieved sparse/materialized snapshot rather than the original study checkout; only 17 repository paths are present and the desktop application/updater source is absent. No conclusion about updater defaults or remote web UI behavior is made."
    ],
    "review_status": "Revised description",
    "review_basis": "publication-decision",
    "proposed_primary_tier": "UNRESOLVED",
    "classification_certified": false,
    "status": "superseded-as-primary-after-systematic-source-retrieval-failures"
  },
  "downloads_and_runs": "yes",
  "transport": "https",
  "payload_verification": "hash + OS-code-signing",
  "trust_root": "OS-trust-store",
  "original_evidence": [
    {
      "file": "batch-O7.md",
      "line": 20,
      "text": "| Requestly (desktop app) | github.com/requestly/http-interceptor-desktop-app (formerly `requestly/requestly-desktop-app`, redirects) | Y (`electron-updater`) | Check: unknown/likely UI-triggered — `autoUpdater.checkForUpdatesAndNotify()` is wired to an `ipcMain.handle(\"check-for-updates-and-notify\", ...)` handler (src/lib/autoupdate/index.js:90) that is only invoked from the remote web app UI loaded into the window at runtime (`getWebAppURL()`), whose source is not in this repo; the equivalent direct call is explicitly commented out in the constructor (line 34: `// autoUpdater.checkForUpdatesAndNotify();`). Download: Y once check runs (electron-updater default `autoDownload=true`, no override found). Install: N automatic — separate `ipcMain.handle(\"quit-and-install\", ...)` (line 95-96) required. | Y | HTTPS — electron-builder `publish` targets GitHub Releases (`owner: \"requestly\", repo: \"requestly-desktop-app\"`) | N override found — no `verifyUpdateCodeSignature` key present, so electron-updater's defaults apply (unlike Firecamp, this one does **not** disable the check) | hash-from-manifest (SHA-512, electron-builder `latest*.yml`) + OS-code-signing (electron-updater default Windows Authenticode publisher-match check, since not disabled) | Windows: publisher name in the Authenticode-signed installer, checked against the previous version's signer by electron-updater's default logic; no override found | src/lib/autoupdate/index.js:1 (`import { autoUpdater } from \"electron-updater\"`), :34 (commented-out direct call), :90 (`autoUpdater.checkForUpdatesAndNotify()` inside IPC handler), :95-96 (`quitAndInstall()` inside separate IPC handler); src/main/main.ts:295 (`new AutoUpdate(webAppWindow)` — only wires listeners, does not itself trigger a check); package.json:150-155 (`\"publish\": {\"provider\": \"github\", \"owner\": \"requestly\", \"repo\": \"requestly-desktop-app\"}`) | Whether the check ever runs automatically depends on code in Requestly's remote web app (loaded via `loadURL`, not in this repo) invoking the IPC channel — can't be confirmed from public source alone. |"
    }
  ]
}
