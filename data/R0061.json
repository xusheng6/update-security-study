{
  "id": "R0061",
  "name": "Mumble (app)",
  "category": "VoIP",
  "historical_tier": "B",
  "project_url": "https://github.com/mumble-voip/mumble",
  "channel": "primary",
  "review_status": "Original Claude Code analysis",
  "proposed_primary_tier": "B",
  "description": "Main-app updater verifies Authenticode before offering to launch.",
  "limitations": [
    "AI-generated static source analysis. Confirm the affected version, platform, update channel, and cited control flow before relying on this classification."
  ],
  "sources": [
    {
      "url": "https://github.com/mumble-voip/mumble",
      "commit": "6d05b026f27b20ba5c26513fba09e1ccf9905b33"
    }
  ],
  "review_basis": "original-claude-code-analysis",
  "snapshot_date": "2026-09-08",
  "classification_certified": false,
  "later_review_audit": {
    "description": "Mumble’s Windows snapshot hashes its binary for the version request and, in the snapshot-install path, calls WinVerifyTrust before showing the update prompt; this narrow observation does not cover other platforms or releases.",
    "limitations": [
      "Newly retrieved default-branch snapshot; not historical release re-verification.",
      "External updater-library behavior and released artifact signing/notarization remain outside the cited source unless directly shown.",
      "Newly retrieved default-branch snapshot; not historical release re-verification.; External updater-library behavior and released artifact signing/notarization remain outside the cited source unless directly shown."
    ],
    "review_status": "Revised description",
    "review_basis": "publication-decision",
    "proposed_primary_tier": "UNRESOLVED",
    "classification_certified": false,
    "status": "superseded-as-primary-after-systematic-source-retrieval-failures"
  },
  "downloads_and_runs": "yes",
  "transport": "https",
  "payload_verification": "OS-code-signing (Authenticode)",
  "trust_root": "OS-trust-store",
  "original_evidence": [
    {
      "file": "batch-U8.md",
      "line": 56,
      "text": "| Mumble | `Mumble (app)` / `Mumble (plugins)` | Main app: Tier B, Windows snapshot builds only — HTTPS-fetched installer verified via `WinVerifyTrust` (Authenticode) before launch, revocation checking disabled (`WTD_REVOKE_NONE`); release builds / non-Windows: N. **Separately, confirmed by direct source read**: Mumble's **plugin auto-updater** (`src/mumble/PluginUpdater.cpp`) downloads a native `.so`/`.dll` from a URL supplied by the plugin itself (`plugin->getUpdateDownloadURL()`), not forced to HTTPS, and hands it straight to `PluginInstaller::install()` with **no hash/signature check anywhere in `PluginInstaller.cpp`**. It does require a user click in a confirmation dialog (`PluginUpdater::promptAndUpdate()`, `exec() == QDialog::Accepted`) before downloading — not fully silent — but the app gives the user zero cryptographic assurance either way. Content-as-code side channel, Tier C-equivalent. Evidence: PluginUpdater.cpp:66-94,355-388; PluginInstaller.cpp (no verify/hash/signature references). |"
    }
  ],
  "structured_evidence": [
    {
      "source_file": "batch-U8.md",
      "source_line": 56,
      "fields": [
        {
          "label": "already covered as",
          "value": "`Mumble (app)` / `Mumble (plugins)`"
        },
        {
          "label": "tier / notable finding",
          "value": "Main app: Tier B, Windows snapshot builds only — HTTPS-fetched installer verified via `WinVerifyTrust` (Authenticode) before launch, revocation checking disabled (`WTD_REVOKE_NONE`); release builds / non-Windows: N. **Separately, confirmed by direct source read**: Mumble's **plugin auto-updater** (`src/mumble/PluginUpdater.cpp`) downloads a native `.so`/`.dll` from a URL supplied by the plugin itself (`plugin->getUpdateDownloadURL()`), not forced to HTTPS, and hands it straight to `PluginInstaller::install()` with **no hash/signature check anywhere in `PluginInstaller.cpp`**. It does require a user click in a confirmation dialog (`PluginUpdater::promptAndUpdate()`, `exec() == QDialog::Accepted`) before downloading — not fully silent — but the app gives the user zero cryptographic assurance either way. Content-as-code side channel, Tier C-equivalent. Evidence: PluginUpdater.cpp:66-94,355-388; PluginInstaller.cpp (no verify/hash/signature references)."
        }
      ]
    }
  ]
}
