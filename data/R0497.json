{
  "id": "R0497",
  "name": "Owncast",
  "category": "Live-streaming server (Go)",
  "historical_tier": "C",
  "project_url": "https://github.com/owncast/owncast",
  "channel": "primary",
  "review_status": "Original Claude Code analysis",
  "proposed_primary_tier": "C",
  "description": "Admin self-updater downloads owncast.online/install.sh via http.Get and pipes it to bash with zero verification — DNS/CDN compromise = RCE.",
  "limitations": [
    "AI-generated static source analysis. Confirm the affected version, platform, update channel, and cited control flow before relying on this classification."
  ],
  "sources": [
    {
      "url": "https://github.com/owncast/owncast",
      "commit": "b0de2e462058b34aa3fa2e68e1939ff416b69de5"
    }
  ],
  "review_basis": "original-claude-code-analysis",
  "snapshot_date": "2026-09-08",
  "classification_certified": false,
  "later_review_audit": {
    "description": "The admin updater downloads install.sh and executes it with bash without payload integrity verification.",
    "limitations": [
      "Source-at-commit evidence only; released artifacts and external framework enforcement remain unverified."
    ],
    "review_status": "Source observation",
    "review_basis": "source-ledger",
    "proposed_primary_tier": "C",
    "classification_certified": false,
    "status": "superseded-as-primary-after-systematic-source-retrieval-failures"
  },
  "downloads_and_runs": "auto",
  "transport": "https",
  "payload_verification": "none",
  "trust_root": "—",
  "original_evidence": [
    {
      "file": "batch-AF3.md",
      "line": 17,
      "text": "| Owncast | https://github.com/owncast/owncast.git | b0de2e4 / 2026-08-17 | Y | Y (admin clicks \"update\", then fully automatic download+exec, no further checks) | Y | https, TLS cert validation NOT disabled in code (uses `http.Get` → Go stdlib default transport/verification; note: server also has a separate opt-in `OWNCAST_INSECURE_SKIP_VERIFY` env var but it is unused by the updater path) | N | none (installer script is piped straight to `bash` with zero hash or signature check) | server-supplied/unpinned (URL is a hardcoded HTTPS endpoint, but the fetched shell script itself is trusted with no verification) | webserver/handlers/admin/update.go:65-96 (`AutoUpdateStart` → `downloadInstaller()` does `http.Get(\"https://owncast.online/install.sh\")`, writes to temp file, then `exec.Command(\"bash\", updater)` with no checksum/signature verification anywhere in the file) |"
    }
  ],
  "structured_evidence": [
    {
      "source_file": "batch-AF3.md",
      "source_line": 17,
      "fields": [
        {
          "label": "repo(clone url)",
          "value": "https://github.com/owncast/owncast.git"
        },
        {
          "label": "commit/date",
          "value": "b0de2e4 / 2026-08-17"
        },
        {
          "label": "has_updater(Y/N)",
          "value": "Y"
        },
        {
          "label": "auto(Y/N)",
          "value": "Y (admin clicks \"update\", then fully automatic download+exec, no further checks)"
        },
        {
          "label": "downloads_and_executes(Y/N)",
          "value": "Y"
        },
        {
          "label": "transport",
          "value": "https, TLS cert validation NOT disabled in code (uses `http.Get` → Go stdlib default transport/verification; note: server also has a separate opt-in `OWNCAST_INSECURE_SKIP_VERIFY` env var but it is unused by the updater path)"
        },
        {
          "label": "manifest_signed",
          "value": "N"
        },
        {
          "label": "payload_verification",
          "value": "none (installer script is piped straight to `bash` with zero hash or signature check)"
        },
        {
          "label": "verifying_key_source",
          "value": "server-supplied/unpinned (URL is a hardcoded HTTPS endpoint, but the fetched shell script itself is trusted with no verification)"
        },
        {
          "label": "evidence(file:line)",
          "value": "webserver/handlers/admin/update.go:65-96 (`AutoUpdateStart` → `downloadInstaller()` does `http.Get(\"https://owncast.online/install.sh\")`, writes to temp file, then `exec.Command(\"bash\", updater)` with no checksum/signature verification anywhere in the file)"
        }
      ]
    }
  ]
}
